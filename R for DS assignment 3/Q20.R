# Create a string with newline, tab, and backslash using escape sequences
my_string <- "Hello,\n\tThis is a string with a newline, tab, and a backslash: \\"
cat(my_string)

my_string <- cat("Line1\n\tLine2 with tab\nLine3 with a backslash: \\")